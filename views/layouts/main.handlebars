<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Reunification Flow</title>
   
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css"
    />
  </head>
  <body>
<section class="hero is-small is-link">
      <div class="hero-body">
        <p class="title">Reunification Flow</p>
        <p class="subtitle">UAC Case Management Supplement Workflow</p>
      </div>
    </section>

          <!-- Conditionally render login or logout links -->
  {{!-- {{#if logged_in}}
  <a href="/cmtracker">profile</a>
    |<button class="no-button" id="logout">logout</button>
  {{else}}
  <div class="field">
  <div class="container field has-addons">
</div></div>
  {{/if}} --}}

    {{{body}}}
  <footer>
    <div class = "container has-text-centered">© 2021 comal software</div>
</footer>
    </div>

    <!-- Render script for logged in users only -->
    {{!-- {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}} --}}
  </body>

</html>